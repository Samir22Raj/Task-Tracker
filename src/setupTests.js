// Test setup removed: this project intentionally excludes test harness files
// to keep the workspace lightweight. Add back `@testing-library/jest-dom`
// and test setup if you want to run unit tests locally.
